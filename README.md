# Pokemons UolEdtech test

## SetUp

Run `npm i` for a install depedencies.
Run `npm start` for a dev server. Navigate to `http://localhost:4200/`.

## web site link

https://pokemons-uoledtech.mateuscosta98dev.now.sh/
